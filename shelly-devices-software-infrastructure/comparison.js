let a = 100;
let b = "100";
// == will compare values only and will convert common types
// === will compare types and values
if (a === b)
   console.log("a equals to b");
else
   console.log("a not equal to b");

// != will compare values
// !== will compare types and values